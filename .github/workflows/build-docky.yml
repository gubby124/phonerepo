name: Build Docky

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/build-docky.yml"
      - "Docky/**"
      - "IconState/**"
      - "tools/update_repo_metadata.py"

permissions:
  contents: write

jobs:
  build:
    runs-on: macos-14
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Select Xcode
        run: |
          sudo xcode-select -s /Applications/Xcode.app/Contents/Developer
          xcodebuild -version

      - name: Install Theos
        run: |
          export THEOS="$HOME/theos"
          brew update || true
          brew install ldid xz || true
          git clone --recursive https://github.com/theos/theos.git "$THEOS"
          mkdir -p "$THEOS/sdks"
          curl -L "https://github.com/theos/sdks/releases/latest/download/iPhoneOS16.5.sdk.tar.xz" | tar -xJ -C "$THEOS/sdks"
          mkdir -p "$THEOS/sdks/iPhoneOS16.5.sdk/System/Library/PrivateFrameworks/Preferences.framework"
          cp "iOS-Blocks/Settings/curago/curago/Preferences.tbd" "$THEOS/sdks/iPhoneOS16.5.sdk/System/Library/PrivateFrameworks/Preferences.framework/Preferences.tbd"
          echo "THEOS=$THEOS" >> "$GITHUB_ENV"

      - name: Build Docky package
        id: build_docky
        working-directory: Docky
        run: |
          set +e
          export THEOS="$HOME/theos"
          make clean 2>&1 | tee ../docky-build.log
          make package FINALPACKAGE=1 TARGET=iphone:clang:16.5:11.0 ARCHS=arm64 2>&1 | tee -a ../docky-build.log
          status=${PIPESTATUS[0]}
          echo "status=$status" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Build IconState package
        id: build_iconstate
        working-directory: IconState
        run: |
          set +e
          export THEOS="$HOME/theos"
          make clean 2>&1 | tee ../iconstate-build.log
          make package FINALPACKAGE=1 TARGET=iphone:clang:16.5:9.0 ARCHS=arm64 2>&1 | tee -a ../iconstate-build.log
          status=${PIPESTATUS[0]}
          echo "status=$status" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Copy build result
        run: |
          mkdir -p docs
          cp docky-build.log docs/docky-build.log
          cp iconstate-build.log docs/iconstate-build.log
          mkdir -p debs
          if [ "${{ steps.build_docky.outputs.status }}" = "0" ]; then
            cp Docky/packages/*.deb debs/
            ls -lh debs/*docky*.deb
          else
            echo "Docky build failed; committed log will show the compiler error."
          fi
          if [ "${{ steps.build_iconstate.outputs.status }}" = "0" ]; then
            cp IconState/packages/*.deb debs/
            ls -lh debs/*iconstate*.deb
          else
            echo "IconState build failed; committed log will show the compiler error."
          fi

      - name: Regenerate Sileo metadata
        run: |
          python3 tools/update_repo_metadata.py --output . --repo-url "https://gubby124.github.io/phonerepo/" --mirror-docs

      - name: Commit built package or build log
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No package changes to commit."
          else
            git commit -m "Build Docky package"
            git push || true
          fi

      - name: Summarize Docky build
        if: always()
        run: |
          {
            echo "### Docky build status: ${{ steps.build_docky.outputs.status }}"
            echo
            echo '```text'
            tail -n 220 docky-build.log || true
            echo '```'
            echo
            echo "### IconState build status: ${{ steps.build_iconstate.outputs.status }}"
            echo
            echo '```text'
            tail -n 220 iconstate-build.log || true
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
          if [ "${{ steps.build_docky.outputs.status }}" != "0" ]; then
            grep -E "error:|fatal error:|Undefined symbols|symbol\\(s\\) not found|framework not found|library not found|ld:|make\\[[0-9]+\\]: \\*\\*\\*" docky-build.log | tail -n 80 | while IFS= read -r line; do
              echo "::error::$line"
            done
          fi
          if [ "${{ steps.build_iconstate.outputs.status }}" != "0" ]; then
            grep -E "error:|fatal error:|Undefined symbols|symbol\\(s\\) not found|framework not found|library not found|ld:|make\\[[0-9]+\\]: \\*\\*\\*" iconstate-build.log | tail -n 80 | while IFS= read -r line; do
              echo "::error::$line"
            done
          fi
